{
  "name": "overlay-v1",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ overlay-market/overlay-v1",
    "create-local": "graph create --node http://localhost:8020/ overlay-market/overlay-v1",
    "remove-local": "graph remove --node http://localhost:8020/ overlay-market/overlay-v1",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 overlay-market/overlay-v1"
  },
  "dependencies": {
    "@amxx/graphprotocol-utils": "^1.1.0-alpha.0",
    "@graphprotocol/graph-cli": "0.22.3",
    "@graphprotocol/graph-ts": "0.22.1"
  }
}
